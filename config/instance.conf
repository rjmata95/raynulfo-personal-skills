# snq — ServiceNow instance registry.
#
# NO SECRETS IN THIS FILE. It is git-tracked. Session cookies and the CSRF
# token live in ~/.config/snq/session (chmod 600), written by `snq auth`.
#
# Override at runtime with SNQ_INSTANCE=https://other.service-now.com

instance = https://chenmed.service-now.com
